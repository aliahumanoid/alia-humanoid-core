name: Firmware CI (PlatformIO)

on:
  push:
    paths:
      - 'software/firmware/**'
      - '.github/workflows/firmware-ci.yml'
  pull_request:
    paths:
      - 'software/firmware/**'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: joint_controller (pico2)
            path: software/firmware/joint_controller
            env: pico2
          - name: joint_controller (pico2_debug)
            path: software/firmware/joint_controller
            env: pico2_debug
          # joint_encoders removed - encoder reading now integrated into joint_controller (Dec 2024)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
          key: pio-${{ runner.os }}-${{ hashFiles('**/platformio.ini') }}

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Build ${{ matrix.name }}
        working-directory: ${{ matrix.path }}
        run: |
          pio run -e ${{ matrix.env }}

